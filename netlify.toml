[build]
  command = "npm run build"
  publish = "dist"

# Це правило важливе для React додатків (SPA). 
# Воно перенаправляє всі запити на index.html, щоб працювала навігація.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
